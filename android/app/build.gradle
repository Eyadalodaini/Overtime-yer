plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.overtime_yer"
    compileSdk = flutter.compileSdkVersion

    defaultConfig {
        applicationId = "com.example.overtime_yer"
        minSdk = 23
        targetSdk = flutter.targetSdkVersion
        versionCode = 1
        versionName = "1.0.0"
    }
}

flutter {
    source = "../.."
}
